# Build output
bin/
obj/
out/
publish/
*.pdb
*.dll
*.exe
*.binlog

# .NET tooling
.dotnet/
.vs/
.idea/
*.user
*.suo
*.userosscache
*.sln.docstates

# NuGet
*.nupkg
*.snupkg
.nuget/
packages/
nuget.config.local

# Test results
[Tt]est[Rr]esults/
*.trx
*.coverage
coverage/

# Husky.NET
.husky/_/

# Lean 4
.lake/
build/
# lake-manifest.json — no-requires project; will start tracking when an external require lands (T024+).
lean/lake-manifest.json

# IDE
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/launch.json
!.vscode/tasks.json

# OS
.DS_Store
Thumbs.db
desktop.ini

# Local env
.env
.env.local
*.local.json

# Editor backup
*~
*.swp
*.swo
WIP.md

# App log (NReco file sink writes to %LOCALAPPDATA%\Stem.ButtonPanelTester\app.log
# at runtime; defensive entry against an `app.log` ever landing inside the worktree
# during dev with a misconfigured CWD or relative log path).
app.log
app.*.log

# resolve-ticket-supervised: local supervisor state (subagents never read it)
llm/supervisor.md
