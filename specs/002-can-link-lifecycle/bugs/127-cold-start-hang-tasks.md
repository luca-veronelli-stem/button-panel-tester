# #127 — PcanCanLink cold-start hang — Tasks

**Branch**: `fix/127-pcan-cold-start-hang` | **Date**: 2026-05-25
**Spec**: [`./127-cold-start-hang.md`](./127-cold-start-hang.md) @ sha `595fde7`
**Plan**: [`./127-cold-start-hang-plan.md`](./127-cold-start-hang-plan.md) @ sha `2e43aa8`
**Spec review**: [`../../../llm/reviews/spec-review.md`](../../../llm/reviews/spec-review.md) (Approved)
**Plan review**: [`../../../llm/reviews/plan-review.md`](../../../llm/reviews/plan-review.md) (Approved)
**Issue**: <https://github.com/luca-veronelli-stem/button-panel-tester/issues/127>
**PR**: <https://github.com/luca-veronelli-stem/button-panel-tester/pull/134>

## Overview

Two durable commits land the fix. Both are vertical (`vertical-commits.md`)
and bisect-safe (`bisect-safe.md`). One out-of-band task tracks the
upstream PR mirror; one manual non-code task records AC7 bench-replay
in the PR description at finalisation.

Format note. `[US1]` = Slice 1 (cold-start fix + AC1); `[US2]` =
Slice 2 (link-layer regression covering AC2 + AC3). Setup phase has no
story label; out-of-band and polish tasks have no story label.

## Acceptance-criteria → slice map

| Spec AC | Slice | Slice commit | Artefact / proof |
|---|---|---|---|
| AC1 — `[<Theory>]` over `driver.IsConnected ∈ {true, false}` at ctor time | Slice 1 | one commit | `tests/ButtonPanelTester.Tests.Windows/Unit/Can/CanPortCtorTests.fs` + per-task RED-then-GREEN evidence captured to `llm/reviews/work-review.md` |
| AC2 — cold-start regression against `PcanCanLink` with fake `ICommunicationPort` | Slice 2 | one commit | `tests/ButtonPanelTester.Tests.Windows/Unit/Can/PcanCanLinkColdStartTests.fs` (`coldStart_FirstOpenAsync_EmitsConnectedOnce`) |
| AC3 — fail-fast path preserved (`Error(Recoverable, _)` exactly once on adapter-absent) | Slice 2 | one commit | `tests/ButtonPanelTester.Tests.Windows/Unit/Can/PcanCanLinkColdStartTests.fs` (`coldStart_FirstOpenAsync_FakePortThrows_EmitsErrorOnce`) |
| AC4 — `VENDOR.md` "Local modifications" row + `VENDOR.sha256` regen | Slice 1 | bundled atomically | `src/ButtonPanelTester.Infrastructure.Protocol/VENDOR.md` row + `src/ButtonPanelTester.Infrastructure.Protocol/VENDOR.sha256` regenerated via `pwsh ./eng/vendor-protocol-stack.ps1 -RehashOnly` |
| AC5 — upstream PR mirror against `luca-veronelli-stem/stem-device-manager` | Out-of-band (no local commit) | n/a | T020 — open PR after Slice 1 lands; URL recorded back into `VENDOR.md` row |
| AC6 — `llm/reviews/gate.ps1` passes | Per-slice handoff (Slice 1 + Slice 2) | each slice | `work-review.md` records exit 0 + build/test/format/lake counts |
| AC7 — bench-replay (PEAK adapter + bus silent → Connected ≤ 2 s) | PR finalisation (manual evidence) | n/a | T021 — recorded in PR description by Owner |

## Phase 1 — Setup (no new infra)

No new project, no new package reference, no new namespace. The plan
inherits the existing `Tests.Windows` (`net10.0-windows`) project +
`Infrastructure.Protocol` (`net10.0-windows`) reference graph. This
phase is intentionally empty; tasks start at the first slice.

## Phase 2 — Slice 1 [US1]: AC1 RED test + `CanPort.cs` ctor fix + vendor manifest (one atomic commit)

**Goal.** Restore the cold-start `Connected` emission for subscribers
that attach after `CanPort` construction, by dropping the ctor-time
`IsConnected` snapshot. Pair the production diff with the RED-then-GREEN
unit test (AC1, two-branch Theory) and the vendor-manifest housekeeping
(AC4) — bundled atomically per `vertical-commits.md` so no intermediate
state is bisect-broken (`bisect-safe.md`).

**Independent test.** `dotnet test tests/ButtonPanelTester.Tests.Windows
-c Release --filter "FullyQualifiedName~CanPortCtorTests"` → 2/2 pass
post-commit; both `InlineData(true)` and `InlineData(false)` rows
observe exactly one `ConnectionState.Connected` emission on a
post-construction subscriber.

**Slice 1 must commit, in one atomic stage:**

- `tests/ButtonPanelTester.Tests.Windows/Unit/Can/CanPortCtorTests.fs`
  (NEW)
- `tests/ButtonPanelTester.Tests.Windows/ButtonPanelTester.Tests.Windows.fsproj`
  (one new `<Compile Include="Unit/Can/CanPortCtorTests.fs" />` row,
  slotted between the existing
  `Unit/InstallationDescriptorProviderTests.fs` (line 18) row and the
  first `Integration/...` row (line 19) — exact placement is cosmetic,
  the file is independent of every sibling)
- `src/ButtonPanelTester.Infrastructure.Protocol/Hardware/CanPort.cs`
  (ctor `_state` assignment, +1/-3 at lines 47-49)
- `src/ButtonPanelTester.Infrastructure.Protocol/VENDOR.md` (new "Local
  modifications" row for `Hardware/CanPort.cs`)
- `src/ButtonPanelTester.Infrastructure.Protocol/VENDOR.sha256`
  (regenerated by `pwsh ./eng/vendor-protocol-stack.ps1 -RehashOnly`)

### Implementation tasks (Slice 1)

- [ ] T001 [US1] Add `Unit/Can/CanPortCtorTests.fs` to the compile list in `tests/ButtonPanelTester.Tests.Windows/ButtonPanelTester.Tests.Windows.fsproj` (one new `<Compile Include="Unit/Can/CanPortCtorTests.fs" />` row, slotted directly after the existing `Unit/InstallationDescriptorProviderTests.fs` row on line 18).
- [ ] T002 [US1] Write the AC1 test file `tests/ButtonPanelTester.Tests.Windows/Unit/Can/CanPortCtorTests.fs` (NEW) — file-private fake `IPcanDriver` (ctor-injected `bool` for `IsConnected`, never-fired `PacketReceived` + `ConnectionStatusChanged` events, `SendMessageAsync = Task.FromResult(true)`, `Disconnect()` no-op, plus a test-only `SetConnected(bool)` shim per plan R5); one `[<Theory>]` over `[<InlineData(true)>]` + `[<InlineData(false)>]`. Body: construct `CanPort(fake)`; attach `StateChanged` handler that captures emissions; flip `SetConnected(true)` for the `(false)` branch; await `ConnectAsync()`; assert the handler observed exactly one `ConnectionState.Connected` and `port.State == Connected`.
- [ ] T003 [US1] **RED evidence.** From the worktree root, run the targeted filter against the pre-fix tree to demonstrate AC1 fails:

      ```powershell
      dotnet test tests/ButtonPanelTester.Tests.Windows -c Release `
          --filter "FullyQualifiedName~CanPortCtorTests"
      ```

      Expected: the `[<InlineData(true)>]` row FAILS with "no
      `StateChanged` emission observed" (because the pre-fix ctor
      snapshots `Connected`, so `ConnectAsync` early-returns at
      `CanPort.cs:73`); the `[<InlineData(false)>]` row passes once
      the test flips the fake mid-test. Capture the failed-row name +
      assertion text into `llm/reviews/work-review.md` under
      `red:`. **Do not commit yet.**

- [ ] T004 [US1] Apply the ctor change in `src/ButtonPanelTester.Infrastructure.Protocol/Hardware/CanPort.cs` (lines 47-49):

      ```diff
      -    _state = (int)(driver.IsConnected
      -        ? ConnectionState.Connected
      -        : ConnectionState.Disconnected);
      +    _state = (int)ConnectionState.Disconnected;
      ```

      Net: +1/-3. Subscription wiring on `:50-51` and ctor
      `ArgumentNullException.ThrowIfNull` on `:45` are unchanged.

- [ ] T005 [US1] **GREEN evidence.** Re-run the same filter from the worktree root:

      ```powershell
      dotnet test tests/ButtonPanelTester.Tests.Windows -c Release `
          --filter "FullyQualifiedName~CanPortCtorTests"
      ```

      Expected: 2/2 pass. Capture `Passed: 2, Failed: 0` into
      `llm/reviews/work-review.md` under `green:`.

- [ ] T006 [US1] Edit `src/ButtonPanelTester.Infrastructure.Protocol/VENDOR.md`: add a row to the "Local modifications" table for `Hardware/CanPort.cs` — `lines: +1, -3`; `why: drop the constructor-time IsConnected snapshot so cold-start ConnectAsync always transitions through Connecting → Connected, restoring FR-001's emission contract for subscribers that attach after construction (issue #127)`; `upstream PR: <placeholder until T020 opens the upstream PR>`. Mirrors the `PCANManager.cs` IAsyncDisposable row's shape (precedent: upstream `stem-device-manager#117`).
- [ ] T007 [US1] Regenerate `src/ButtonPanelTester.Infrastructure.Protocol/VENDOR.sha256` from the worktree root:

      ```powershell
      pwsh ./eng/vendor-protocol-stack.ps1 -RehashOnly
      ```

      The script computes `$repoRoot` from `$PSScriptRoot` so it is
      CWD-tolerant (per plan R1). Confirm the diff for
      `VENDOR.sha256` shows a single hash row change against
      `Hardware/CanPort.cs`.

- [ ] T008 [US1] Run the local gate from the worktree root and record the result into `llm/reviews/work-review.md` (`gate:` line — full command + exit code + build/test/format/lake counts):

      ```powershell
      pwsh ./llm/reviews/gate.ps1
      ```

      Expected: exit 0; build green; `Tests` (61 passed) +
      `Tests.Windows` (118 passed, +2 vs. baseline of 116 thanks to
      the two new AC1 Theory rows, 3 hardware-skipped); format clean;
      lake build green. If any leg fails, return to the offending
      task and fix in place — do not advance.

- [ ] T009 [US1] Stage all five paths atomically and create the slice-1 commit. Suggested title (reviewer will refine and supply final title/body at `WorkApproved`):

      ```text
      fix(infrastructure-protocol): #127 drop CanPort ctor IsConnected snapshot
      ```

      Body talking points the worker must include (per plan F1 +
      `github-pr-auto-close.md`):

      - Cite `CanPort.cs:43-52` for the ctor; `:73` for the
        early-return branch that masks the missing emission; `:78-82`
        for the `IsConnected` poll branch that now fires `Transition(Connected)`
        on attempt 0 against the now-subscribed handler.
      - Cite `PCANManager.cs:72-83` (setter), especially `:80` (the
        `?.Invoke(this, _isConnected)`), for the event-fire site that
        is dropped on the floor when no subscriber has attached. Also
        cite `:85-90` (ctor) and `:97-100` (`Initialize` body) for the
        eager-`Initialize` trigger that flips `IsConnected` to `true`
        synchronously inside the `PCANManager` constructor.
      - Cite `PcanCanLink.fs:235-264` (`ensurePort`), specifically
        `:244` (`port.StateChanged.AddHandler handler`), for the
        subscription attach that happens after `portFactory ()`
        returns — i.e. *after* the dropped emission.
      - Vendor manifest discipline: new "Local modifications" row in
        `VENDOR.md`; `VENDOR.sha256` regenerated via
        `pwsh ./eng/vendor-protocol-stack.ps1 -RehashOnly`; upstream
        PR URL placeholder until T020 opens it (matches
        `PCANManager.cs`'s IAsyncDisposable diff precedent).
      - **`Refs #127`, NOT `Closes #127`.** Rebase-merge replays every
        commit body onto `main`; auto-close keywords in any commit
        body trigger auto-close on merge regardless of the PR body,
        and this PR is gate-step 2 of 7 (`#125 → #127 → #124 → #130 →
        #131 → #128 → #129`) — the closing keyword belongs in the PR
        body only (see `~/.claude/rules/github-pr-auto-close.md`).
      - No AI attribution (`~/.claude/rules/no-attribution.md`).

      After commit, write `llm/reviews/work-review.md` with `sha`,
      `slice: 1 (AC1 + fix + vendor)`, `red` / `green` / `gate` lines
      populated, and set the volatile state to `WorkReview`. **No push.**

**Checkpoint (Slice 1).** Bench-target invariant restored: any post-
construction subscriber to `CanPort.StateChanged` receives the
`Connected` transition once on the cold-start success path. AC4 is
satisfied. AC6 recorded green.

---

## Phase 3 — Slice 2 [US2]: AC2 + AC3 link-layer regression (test-only commit)

**Goal.** Lock the `PcanCanLink` cold-start contract in place against
the post-fix `ICommunicationPort` shape — `Initializing → Connected`
once per first `OpenAsync` (AC2), and the existing adapter-absent
fail-fast path (`Error(Recoverable, _)` exactly once via
`LinkStateChanged`, no surfaced exception) preserved (AC3). Bisect-safe
by construction: no production code changes.

**Independent test.** `dotnet test tests/ButtonPanelTester.Tests.Windows
-c Release --filter "FullyQualifiedName~PcanCanLinkColdStartTests"` →
2/2 green-on-arrival (these are regression fixtures against the
post-Slice-1 contract; no RED phase is expected because Slice 1 already
restored the underlying emission the link wrapper translates).

**Slice 2 must commit, in one atomic stage:**

- `tests/ButtonPanelTester.Tests.Windows/Unit/Can/PcanCanLinkColdStartTests.fs`
  (NEW)
- `tests/ButtonPanelTester.Tests.Windows/ButtonPanelTester.Tests.Windows.fsproj`
  (one new `<Compile Include="Unit/Can/PcanCanLinkColdStartTests.fs" />`
  row, slotted directly after the Slice-1 row)

### Implementation tasks (Slice 2)

- [ ] T010 [US2] Add `Unit/Can/PcanCanLinkColdStartTests.fs` to the compile list in `tests/ButtonPanelTester.Tests.Windows/ButtonPanelTester.Tests.Windows.fsproj` (new `<Compile Include="Unit/Can/PcanCanLinkColdStartTests.fs" />` row, immediately after the Slice-1 row added in T001).
- [ ] T011 [US2] Write the AC2 + AC3 test file `tests/ButtonPanelTester.Tests.Windows/Unit/Can/PcanCanLinkColdStartTests.fs` (NEW) — file-private fake `ICommunicationPort` per plan §"Phase 1 — Design" (Slice 2):
    - `Kind = ChannelKind.Can`;
    - `State` initially `Disconnected` (post-fix shape);
    - `IsConnected = State == Connected`;
    - `PacketReceived` + `StateChanged` as `Event<_>` instances;
    - `ConnectAsync` scriptable — one variant fires `StateChanged(Connected)` synchronously then returns; the other raises `InvalidOperationException` *after* synchronously firing `StateChanged(Error)` (mirroring `CanPort.cs:85-87`'s fail-fast contract);
    - `DisconnectAsync` / `SendAsync` / `Dispose` no-op (not exercised by these tests).

    Two `[<Fact>]` cases:

    - `coldStart_FirstOpenAsync_EmitsConnectedOnce` (AC2): construct `PcanCanLink((fun () -> fakePort), NullLogger<PcanCanLink>.Instance)`; subscribe to `LinkStateChanged`; await `OpenAsync(250_000, CancellationToken.None)`; the fake's `ConnectAsync` fires one `StateChanged(Connected)` then returns. Assert `LinkStateChanged` emitted exactly one `Connected(_, _)`; `CurrentState` reads `Connected(_, _)`.
    - `coldStart_FirstOpenAsync_FakePortThrows_EmitsErrorOnce` (AC3): same setup but the fake's `ConnectAsync` synchronously fires `StateChanged(Error)` then throws `InvalidOperationException`. Assert `LinkStateChanged` emitted exactly one `Error(Recoverable _, _)`; `CurrentState` reads `Error(Recoverable _, _)`; no exception escapes `OpenAsync` (per `PcanCanLink.openInternal`'s "failures surface via `LinkStateChanged`, not throws" contract).

- [ ] T012 [US2] **Green-on-arrival evidence.** From the worktree root, run the targeted filter:

      ```powershell
      dotnet test tests/ButtonPanelTester.Tests.Windows -c Release `
          --filter "FullyQualifiedName~PcanCanLinkColdStartTests"
      ```

      Expected: 2/2 pass. Capture `Passed: 2, Failed: 0` and the
      individual test names into `llm/reviews/work-review.md` under
      `green:`. Note in `red:` that no RED phase is expected because
      Slice 1 already restored the underlying emission contract this
      slice regresses — this is a documented exception to the
      RED-then-GREEN ordering per `resolve-ticket` SKILL.md ("test/proof
      and implementation together, or a reviewer explicitly documents
      an exception"). Reviewer to confirm at `WorkReview`.

- [ ] T013 [US2] Run the local gate from the worktree root and record the result into `llm/reviews/work-review.md` (`gate:` line — full command + exit code + build/test/format/lake counts):

      ```powershell
      pwsh ./llm/reviews/gate.ps1
      ```

      Expected: exit 0; build green; `Tests` (61 passed) +
      `Tests.Windows` (120 passed, +2 vs. Slice-1 baseline of 118, 3
      hardware-skipped); format clean; lake build green. If any leg
      fails, return to T011/T012 and fix in place — do not advance.

- [ ] T014 [US2] Stage both paths atomically and create the slice-2 commit. Suggested title (reviewer will refine and supply final title/body at `WorkApproved`):

      ```text
      test(infrastructure): #127 regression — PcanCanLink cold-start + fail-fast
      ```

      Body talking points the worker must include:

      - Names both fixtures and what they regress (AC2 cold-start
        emission once; AC3 fail-fast `Error(Recoverable, _)` emission
        once with no surfaced exception).
      - Cites the post-fix `ICommunicationPort` shape the fake mirrors
        and `PcanCanLink.openInternal`'s "failures surface via
        `LinkStateChanged`, not throws" invariant.
      - **`Refs #127`, NOT `Closes #127`** (same auto-close discipline
        as Slice 1; see `~/.claude/rules/github-pr-auto-close.md`).
      - No AI attribution.

      After commit, update `llm/reviews/work-review.md` with the new
      `sha`, `slice: 2 (AC2 + AC3 regression)`, and set the volatile
      state to `WorkReview`. **No push.**

**Checkpoint (Slice 2).** Link-layer regression locks AC2 + AC3 in
place against the post-fix port contract. AC6 recorded green.

---

## Phase 4 — Out-of-band & polish (no local commits)

- [ ] T020 **Out-of-band: upstream PR mirror (AC5).** *Not part of the local commit chain.* After Slice 1 lands locally, open a PR against `luca-veronelli-stem/stem-device-manager` carrying the same `CanPort.cs` one-liner + an xUnit C# regression test against a fake `IPcanDriver` (mirroring the AC1 Theory shape, but in xUnit C# per upstream's test stack). Record the upstream PR URL back into `src/ButtonPanelTester.Infrastructure.Protocol/VENDOR.md`'s "Local modifications" row. The URL update can ride as a small `--amend` of the Slice-1 commit during review (per the `pr` skill's "in-place fix" rule) or as a follow-up doc-only durable commit during finalisation — worker chooses based on timing with the upstream PR's creation. Per the IAsyncDisposable precedent (`stem-device-manager#117`), the upstream PR does **not** need to be merged before #127 lands locally; same gate.
- [ ] T021 **Manual evidence: bench-replay (AC7).** *Non-code, recorded at finalisation.* During PR finalisation, the Owner records bench-replay evidence in the PR description: launch the app with the PEAK PCAN-USB Pro FD adapter plugged in and bus silent; the CAN status row must reach `Connected` within the SC-001 budget (≤ 2 s after dictionary warm-up completion). Artefact: a screenshot of the status row at `Connected` plus a log excerpt from `%LOCALAPPDATA%\Stem\ButtonPanelTester\logs\app.log` showing the post-warm-up `PcanCanLink.OpenAsync → Connected` transition timestamps. This is the only ACE not covered by automated tests.
- [ ] T022 **Polish: PR-D #116 rebase note (downstream concern).** *Out-of-band, recorded for Owner visibility.* This PR is gate-step 2 of 7 in the spec-002 fix-before-PR-D roadmap (`#125 → #127 → #124 → #130 → #131 → #128 → #129`). After this PR merges, PR-D #116 must rebase onto the new `main`. The diff is +1/-3 in `Infrastructure.Protocol/Hardware/CanPort.cs` (a section PR-D does not touch) and two new files under `Tests.Windows/Unit/Can/` (PR-D does not touch either) — per plan R3, rebase impact is negligible. The Owner should mention this downstream coordination in the PR description, but the rebase itself is not the worker's responsibility on this PR. After this PR merges, queue step 3 (`#124`) per the roadmap memory.

---

## Constitution gate (per plan §"Constitution Check")

The tasks below do not introduce any new violation of the six
constitutional principles validated by the plan review:

- **I. Formal Verification of Invariants** — Defect-fix track; no Lean
  module update mandated. Tasks T001–T014 land C# + F# + manifest
  changes only.
- **II. Property-Driven Correctness** — AC1 is a `[<Theory>]` over the
  two-valued ctor input domain (T002); AC2/AC3 are
  contract-fixture `[<Fact>]`s (T011). Rationale documented in the
  plan; no FsCheck property is required.
- **III. Ports and Adapters for Every External Boundary** — Both fakes
  are hand-rolled and file-private (T002, T011). No mocking library.
- **IV. CI Greens the Whole Stack; Hardware Tests Are Explicit** —
  All new tests are hardware-free (`Unit/Can/`). No new `Category=Hardware`
  trait. Gate run before each handoff (T008, T013). AC7's hardware
  evidence is manual and recorded in the PR description (T021).
- **V. Supplier-Deployed Identity Is Hashed at Capture** — N/A; no
  identity field on this path.
- **VI. Stopgap Discipline** — The `CanPort.cs` modification rides the
  upstream-PR-paired vendor-fix precedent (T020 opens the mirror).
  Not a stopgap; no waiver required.

## Per-commit checklist (run for both slice commits)

Each task that introduces a commit (T009, T014) must satisfy:

- [ ] All staged paths form a single bisect-safe state (build + tests
      + format + lake all green via T008 / T013).
- [ ] Commit title is Conventional Commits, English, imperative mood,
      lowercase after the colon.
- [ ] Commit body uses **`Refs #127`**, NOT `Closes #127`. The closing
      keyword lives in the PR body only, per
      `~/.claude/rules/github-pr-auto-close.md`. (Rebase-merge replays
      every commit body onto `main`; `Closes #127` in any commit body
      auto-closes the issue regardless of the PR body, and #127 must
      stay open across the remaining roadmap steps until all
      regressions land.)
- [ ] No AI attribution anywhere in title or body
      (`~/.claude/rules/no-attribution.md`).
- [ ] No semantic changes during message-only amendments at
      `WorkApproved` — only the title/body the reviewer supplies in
      `state.md.approved_commit`.

## Dependency graph

```text
T001 ─┐
      ├─► T002 ─► T003 (RED) ─► T004 ─► T005 (GREEN) ─► T006 ─► T007 ─► T008 (gate) ─► T009 (commit Slice 1)
                                                                                         │
                                                                                         ▼
                                                                                       T010 ─┐
                                                                                             ├─► T011 ─► T012 (green) ─► T013 (gate) ─► T014 (commit Slice 2)
                                                                                                                                          │
                                                                                                                                          ▼
                                                                                                                                        T020 (upstream PR — out-of-band)
                                                                                                                                        T021 (AC7 manual — finalisation)
                                                                                                                                        T022 (PR-D rebase note — Owner-visible)
```

T001–T009 must execute strictly in order (Slice 1's RED-then-GREEN
discipline depends on the test landing before the fix in working-tree
state, even though the commit is atomic). T010–T014 run after T009
lands but never in parallel with it. T020 / T021 / T022 are post-merge
or out-of-band; the worker does not block on them.

## Independent test summary (per slice)

| Slice | Filter | Expected result | Records into |
|---|---|---|---|
| Slice 1 (post-commit) | `dotnet test tests/ButtonPanelTester.Tests.Windows -c Release --filter "FullyQualifiedName~CanPortCtorTests"` | 2/2 pass | `work-review.md` `green:` (T005) |
| Slice 2 (post-commit) | `dotnet test tests/ButtonPanelTester.Tests.Windows -c Release --filter "FullyQualifiedName~PcanCanLinkColdStartTests"` | 2/2 pass | `work-review.md` `green:` (T012) |
| Full gate (each handoff) | `pwsh ./llm/reviews/gate.ps1` | exit 0, build + tests + format + lake green | `work-review.md` `gate:` (T008, T013) |

## Implementation strategy

Land Slice 1 first; reviewer approves at `WorkReview`; worker amends
the approved title/body verbatim at `WorkApproved`. Then land Slice 2;
reviewer approves; worker amends. Reviewer escalates to
`FinalizationRequired`. Owner sets PR title/body/labels, records the
AC7 bench-replay (T021), references the PR-D rebase note (T022),
ensures the upstream PR URL is recorded in `VENDOR.md` (T020 done or
pending per the IAsyncDisposable precedent), drops the volatile
`llm/reviews/*` commit, and merges (or asks Luca to merge) with
**rebase**, not squash (per the standing rebase-over-squash
preference). The next roadmap step (`#124`) starts after this PR's
merge.
