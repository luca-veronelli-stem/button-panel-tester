module Stem.ButtonPanelTester.Core.Placeholder

/// Marker proving the bootstrap stub is wired into the build.
/// Replaced when the first real Core types land via speckit-plan.
[<Literal>]
let markerVersion = "v0.0.0-bootstrap"
