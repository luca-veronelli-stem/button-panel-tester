# Session handoff — Phase B spec-002 (home → work, 2026-05-27)

**Direction**: home machine → work machine. Inverse of `HANDOFF.md`, which carried 2026-05-26 work → home.

**Branch**: `docs/002-lifecycle-spec-refresh`. Tip: **`8327e05`** (`docs(spec-002): rewrite spec.md against five-state FSM (Phase B)`). Pushed to `github`. Commit `3150c3f` (HANDOFF + PROMPT scratch) is the parent.

**Status**: spec.md landed. The other six artefacts are queued behind Luca's review of spec.md (he chose "spec.md first, then await review" — the smaller-blast-radius option from the parked §7 question).

**This file is throwaway** along with `HANDOFF.md` and `PROMPT.md` — all three get `git rm`d in the final cleanup commit before the PR opens (see original `HANDOFF.md` §11).

---

## 1 — How to resume on the work machine

```powershell
Set-Location C:\Users\LucaV\Source\Repos\button-panel-tester
git fetch github
git checkout docs/002-lifecycle-spec-refresh
git pull github docs/002-lifecycle-spec-refresh
```

Then read in this order:

1. **This file** (`HANDOFF-2026-05-27.md`) — what changed in the home session + judgment calls I made + queue state.
2. **`HANDOFF.md`** — original 2026-05-26 work-machine session notes. Still load-bearing for the §6 FSM design (this rewrite implements it verbatim) and the §8 artefact plan (still authoritative for the remaining six).
3. **`specs/002-can-link-lifecycle/spec.md`** at `8327e05` — what got written.

`PROMPT.md` was the home-machine resume prompt; it's no longer load-bearing (resume context is now spec.md itself + this handoff).

---

## 2 — Greenlight question Luca answered before I wrote

From the FSM clarification:

> §6.2 says Reconnect from `Faulted` → `Opening (same candidate)`, but §6.3's `Faulted of cause * since` DU carries no candidate. Where should the candidate memory live?

Luca picked **"Carry candidate in Faulted payload"** — chosen DU shape:

```fsharp
| Faulted of cause: FaultCause
           * candidate: AdapterCandidate option
           * since: DateTimeOffset
```

Reconnect typed transitions:

```fsharp
Faulted (_, Some c, _) -> Opening (c, now)
Faulted (_, None,   _) -> Searching (Polling, now)
```

Rationale captured in spec.md's 2026-05-27 clarifications section. **Update `HANDOFF.md` §6.3 mental model when reading it** — `Faulted` is now richer than the original §6.3 sketch.

On write cadence Luca picked **"spec.md first, then await review"** — that's why the other six artefacts are still queued.

---

## 3 — What landed in spec.md (commit `8327e05`)

- **Section order** matches `.specify/templates/spec-template.md` plus the substrate's `Clarifications` and `Presentation surfaces` extensions.
- **Two user stories** (per PROMPT.md). US1 = visibility at start of shift; US2 = mid-session resilience covering Stop/Start, unplug, bus-off, busy-adapter, driver-missing. The substrate's US3 was absorbed into US2.
- **15 FRs**, FR-001 .. FR-015, organised as: FSM and lifecycle (FR-001 .. FR-006) + user affordances (FR-007 .. FR-010) + enumeration (FR-011) + boundary (FR-012 .. FR-015).
- **9 SCs**, SC-001 .. SC-009. SC-009 is new — measures FR-011's multi-adapter iteration at bench level.
- **Six clarification session bullets**: five carried forward (2026-05-24 / -25 / -26, bench truth) + new 2026-05-27 (FSM redesign).
- **Out of Scope** picks up two fresh items: multi-adapter selection UI (FR-011 makes the contract iterating-first-available, so a selection UI is explicitly future) and Stop-state persistence across restarts.

---

## 4 — Judgment calls I made (worth a once-over before the other six fan out)

These are choices that weren't pinned by HANDOFF.md §6 or Luca's clarifications. If any read wrong, **amend `8327e05` in-place** rather than fixing-at-the-tip — see `~/.claude/skills/pr/SKILL.md` in-place-fix rule.

| # | Call | Where | Rationale | Easy to change? |
|---|------|-------|-----------|------------------|
| 1 | US3 dropped, merged into US2 | spec.md "User Scenarios & Testing" | PROMPT.md said "two user stories"; Stop/Start + unplug + faults are all the same theme (mid-session resilience). | Yes — split US2 if reviewer wants the user-driven Stop/Start to read as its own US3. |
| 2 | `Idle(AwaitingBoot)` Start button is visible-but-disabled with tooltip, not hidden | FR-008, Acceptance Scenario 1 of US1 | Bench convention: affordance present so user understands the wait, rather than mysteriously absent. The substrate hid Reconnect in `Initializing`; for Start in the new FSM I went the other way. | Yes — flip to hidden if reviewer prefers symmetry with the substrate hide rule. |
| 3 | `Reconnect (resume scanning)` caption when `Faulted(_, None, _)` | FR-009 | The button effect changes (retry vs re-scan), and the user shouldn't be surprised. SHOULD only — phrasing is GUI design. | Yes — drop the SHOULD to a Note if reviewer wants pure FSM semantics in the spec. |
| 4 | `since` rule (FR-005) explicitly covers discriminator-only changes within a family | FR-005 | Substrate's FR-002b only spoke to Error. Generalising to all states required deciding whether `Searching(NoAdapterEnumerated) → Searching(Polling)` ticks `since`. I said yes (it's a different cause), to preserve information value at the affordance. | Yes — narrow to family-change-only if reviewer wants the substrate behaviour preserved. |
| 5 | Cause/suggestion em-dash convention framed as transitional in the NotificationCenter row of "Presentation surfaces" | "Presentation surfaces" table + Dependencies | Substrate's session 2026-05-26 said this convention is a pragmatic interim; substrate language stayed in the FR. I lifted the transitional framing up to the surfaces table so a future spec doesn't have to re-derive it. | Trivial — just a place to put it. |

None of these block fanning out to the other six artefacts. They're listed here so the work-machine session can either confirm or amend before that fan-out.

---

## 5 — Queue (still-to-write, commit order from HANDOFF.md §8)

Each is a separate vertical commit on `docs/002-lifecycle-spec-refresh`. Doc-only — skip the local build / test gate (Luca's memory `feedback_skip_local_gate_for_docs.md`).

1. **data-model.md** — F# DU per HANDOFF.md §6.3 *plus the candidate-in-Faulted addition*. Mermaid state diagram of §6.2 edges. Lean Phase 2 cross-references: `state_classification_total` over five families, `fault_cause_total`, `idle_cause_total`. Search-attempt theorems if natural.
2. **research.md** — decisions log: Idle inclusion (§4b), drop Recoverable / Fatal (§4c), multi-adapter iteration (§4a), hot-plug as explicit edge (§6.2), Searching retry cadence (§6.8), NotificationCenter deferred (§4d), v0.1.0 mental baseline (§4e), candidate-in-Faulted (new 2026-05-27 clarification).
3. **plan.md** — Constitution Check re-run, Project Structure unchanged (archetype A, two-TFM split), Lean Phase 2 re-prove plan (theorem count grew from 2 → 3 or 4), Searching retry cadence pinned (was deferred to plan per §6.8 — recommend 5 s poll with vendored-stack event as the fast path, but pick freely).
4. **tasks.md** — renumbered T001 onwards, lifecycle-only. The substrate's shared Phase 2 foundation tasks (T012 .. T033) are not retained — those are already merged on `main`; the new tasks.md is forward-looking only.
5. **quickstart.md** — refreshed bench walkthrough. Cover: cold start to `Open`, Stop / Start path, Reconnect from `Faulted(BusOff)`, driver-missing path, hot-plug recovery, multi-adapter iteration (if a second bench adapter exists; otherwise note it as "skip if you only have one").
6. **migration-map.md** — old → new table per HANDOFF.md §8. Load-bearing for Phase C. The placeholder rows in HANDOFF.md §8 still apply; fill in the real new-FR numbers (FR-001 .. FR-015 — they're now pinned by spec.md).

Then the final commit:

7. **cleanup commit**: `git rm HANDOFF.md PROMPT.md HANDOFF-2026-05-27.md` + commit as `chore: drop Phase B session handoff artefacts`.

Then push and open the PR per HANDOFF.md §11 — github only, label `docs`, title `docs(spec-002): rewrite lifecycle spec via /speckit (Phase B)`, body links to migration-map.md and names the FSM redesign.

---

## 6 — Open invitations for Luca

If any of the §4 judgment calls read wrong, the in-place fix on `8327e05` is cheap right now. After the next six artefacts cross-reference spec.md by FR number, amending it is more disruptive.

If Luca wants to **rerun `/speckit-analyze`** between spec.md and the other six (or between any two of the seven), that's the kind of mid-spec guardrail his memory `feedback-workflow-selection.md` welcomes.

If Luca wants the **NotificationCenter spec landed first** (per HANDOFF.md §4d brainstorm), say so before data-model.md commits — the row's "carry both cause and suggestion" framing is transitional in spec.md and would tighten if NotificationCenter was upstream.

---

## 7 — Repo state at the time of this handoff

```text
* 8327e05 (HEAD -> docs/002-lifecycle-spec-refresh, github/docs/002-lifecycle-spec-refresh) docs(spec-002): rewrite spec.md against five-state FSM (Phase B)
* 3150c3f                                                                                  chore: Phase B session handoff scratch (HANDOFF + PROMPT)
* 5198a34 (github/main)                                                                     refactor(specs): #151 split spec-002 into lifecycle + new spec-003
```

Working tree clean. Branch is up to date with `github`. `bitbucket` will not see this branch (mirror runs on `main` only).

Memories that still need an update **after the rewrite lands** (per HANDOFF.md §10):

- `spec-002-live-roadmap.md` — bump `Last updated`, add Phase B done entry, note the FSM reshape + the Lean re-prove cost (now `Faulted` candidate option to model too).
- `spec-003-live-roadmap.md` — note that `LinkStateChanged` payload shape changed (5-family) and spec-003 FR-015' needs re-derive.

These memories live under `~/.claude/projects/C--Users-LucaV-Source-Repos-button-panel-tester/memory/` and are work-machine only.
